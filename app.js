const $=s=>document.querySelector(s);let photos=[];let objectUrls=[];const imageExt=/\.(jpe?g|png|webp|gif|bmp|avif)$/i;function clean(){objectUrls.forEach(URL.revokeObjectURL);objectUrls=[]}async function walk(dir,prefix=''){const out=[];for await(const [name,h] of dir.entries()){if(name.startsWith('.'))continue;if(h.kind==='directory')out.push(...await walk(h,prefix+name+'/'));else if(imageExt.test(name)){const f=await h.getFile();out.push({name,path:prefix+name,size:f.size,lastModified:f.lastModified,handle:h,file:f})}}return out}function monthKey(ts){const d=new Date(ts);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}function render(){clean();const q=$('#q').value.toLowerCase();const list=photos.filter(p=>p.path.toLowerCase().includes(q)).sort((a,b)=>b.lastModified-a.lastModified);const groups=Object.groupBy?Object.groupBy(list,p=>monthKey(p.lastModified)):list.reduce((a,p)=>((a[monthKey(p.lastModified)]??=[]).push(p),a),{});$('#main').innerHTML=Object.entries(groups).map(([m,arr])=>`<section class=month><h2>${m} <small>(${arr.length})</small></h2><div class=grid>${arr.map(p=>{const u=URL.createObjectURL(p.file);objectUrls.push(u);return `<div class=tile title="${esc(p.path)}"><img loading=lazy src="${u}"><span>${esc(p.name)}</span></div>`}).join('')}</div></section>`).join('')||'<p>No photos.</p>';$('#status').textContent=`${photos.length} photos indexed locally.`}async function hashFile(f){return [...new Uint8Array(await crypto.subtle.digest('SHA-256',await f.arrayBuffer()))].map(x=>x.toString(16).padStart(2,'0')).join('')}$('#open').onclick=async()=>{if(!window.showDirectoryPicker)return alert('Use Chrome or Edge on desktop.');const d=await showDirectoryPicker();$('#status').textContent='Scanning…';photos=await walk(d);render()};$('#q').oninput=render;$('#dupes').onclick=async()=>{if(!photos.length)return;$('#status').textContent='Hashing files…';const bySize={};for(const p of photos)(bySize[p.size]??=[]).push(p);const groups=[];for(const arr of Object.values(bySize).filter(x=>x.length>1)){const hm={};for(const p of arr){const h=await hashFile(p.file);(hm[h]??=[]).push(p)}for(const g of Object.values(hm))if(g.length>1)groups.push(g)}$('#dupeList').innerHTML=groups.length?groups.map(g=>`<div class=dupe><b>${g.length} identical files</b><ul>${g.map(p=>`<li>${esc(p.path)}</li>`).join('')}</ul></div>`).join(''):'<p>No exact duplicates found.</p>';$('#dlg').showModal();$('#status').textContent=`${groups.length} duplicate groups.`};$('#export').onclick=()=>{const data=photos.map(({path,size,lastModified})=>({path,size,lastModified}));const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='photovault-index.json';a.click()};function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
if('serviceWorker' in navigator) navigator.serviceWorker.register('./service-worker.js').catch(()=>{});
